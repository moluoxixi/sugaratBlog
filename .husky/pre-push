#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# 同步子模块
echo "🔄 自动同步子模块..."
pnpm sync:submodule

# 检查是否有错误
if [ $? -ne 0 ]; then
  echo "❌ 子模块同步失败，推送已取消"
  exit 1
fi

echo "✅ 子模块同步完成，继续推送..."

